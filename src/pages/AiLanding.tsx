// The /ai page moved to src/pages/ai/ (composition + data + i18n + components).
// This shim keeps the lazy-route import path in route-prefetch.ts stable.
export { AiLanding } from './ai/AiLanding';
